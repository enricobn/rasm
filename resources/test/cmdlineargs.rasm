include "std.rasm"

printargs();

fn printargs() {
    nprintln(argc());
    printargv(1);
}

fn printargv(n: i32) {
    if(lessOrEqual(n,argc()),{
        sprintln(argv(n));
        printargv(nadd(n, 1));
    },{});
}