include "std.rasm"

cprintln('a');
cprintln('è');
cprintln('😀');
bprintln(ceq('A', 'A'));
bprintln(ceq('A', 'b'));

//listPrintln(lines("first\nsecond\nthird\n☀\n"), { it -> sprint(it); });
listPrintln(chars("Hello"), { it -> sprint(it); });