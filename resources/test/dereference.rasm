include "std.rasm"

let r = range(0, 2);
bprintln(anyMatch(range(0,2), { n -> eq(n, 0);}));
bprintln(anyMatch(r, { n -> eq(n, 0);}));
let r1 = range(0, 2);
bprintln(anyMatch(range(0,2), { n -> eq(n, 2);}));
bprintln(anyMatch(r1, { n -> eq(n, 2);}));
let r2 = range(0, 2);
bprintln(anyMatch(range(0,2), { n -> eq(n, 0);}));
bprintln(anyMatch(r2, { n -> eq(n, 0);}));