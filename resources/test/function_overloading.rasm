include "std.rasm"

sprintln(myToString(10));
sprintln(myToString("Hello"));
sprintln(add("Hello", " world"));
nprintln(add(1, 10));

fn myToString(s: str) -> str {
    it(s);
}

fn myToString(n: i32) -> str {
    it("a number");
}