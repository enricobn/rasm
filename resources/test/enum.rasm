include "std.rasm"

enum Option<T> {
  Some(value : T),
  None
}

printOption(Option::Some("a value"));
printOption(Option::None());
printOption(Option::Some("a value"));
printOption(Option::None());

fn printOption(o: Option<T>) {
    OptionMatch(o, {sprintln("some");}, {sprintln("none");});
}