pub struct HighScore {
    score: int,
    time: TimeVal
}

fn printHighScores(highScores: Vec<HighScore>) {
    highScores.forEach(fn(highScore) {
        print("high score ", highScore.scorre)
        print(", ")
        println(highScore.time)
    })
}