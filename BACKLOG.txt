Doing
------
- optimizing inline for fibonacci
    - move more code as possible from codegen to function_call_parameters
    - fix tests
    - can we inline variables when called from standard or lambda functions?
      example:
      fn fib(n: i32) -> i32 {
          if (lessOrEqual(n, 1),{
              itn(n);
          },{
              nadd(fib(nadd(n,-1)), fib(nadd(n,-2)));
          });
      }

      can we inline n in itn and nadd calls?

- integration tests that compile a program, run it and check the output