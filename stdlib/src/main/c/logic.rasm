pub native or(a: bool, b: bool) -> bool /{
    $inline()
    return ($a || $b);
}/

pub native and(a: bool, b: bool) -> bool /{
    $inline()
    return ($a && $b);
}/

pub native not(a: bool) -> bool /{
    $inline()
    return (!$a);
}/