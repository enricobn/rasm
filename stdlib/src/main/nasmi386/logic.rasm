pub fn or(a: bool, b: bool) -> bool /{
    $inline()
    mov     eax,$a
    or      dword eax,$b
}/

pub fn and(a: bool, b: bool) -> bool /{
    $inline()
    mov     eax,$a
    and     dword eax,$b
}/

pub fn not(a: bool) -> bool /{
    $inline()
    mov     eax,$a
    xor     dword eax,1
}/