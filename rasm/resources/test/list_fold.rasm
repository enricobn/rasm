let n = toInt(argv(1)).getOrElse(999);

println(foldLeft(range(0, n), 0, fn(prev, actual) { add(prev, add(prev, actual)); }));

println(foldRight(range(0, n), 0, fn(prev, actual) { add(prev, add(prev, actual)); }));
