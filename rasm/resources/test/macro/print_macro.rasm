let h = "Joe";
let w = "how do you do";

println!("Hello {}, {}{}", h, w, "?");

range(1, 2)
.forEach(fn(i) { println!("Number {}", i); });

printInFunction(10);

fn printInFunction(n: int) {
    println!("an int {}", n);
}