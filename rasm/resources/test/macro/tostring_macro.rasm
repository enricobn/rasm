@toString()
pub struct Circle {
    center: Point,
    radius: int
}

@toString()
pub struct Point {
    x: int,
    y: int
}

@toString()
pub enum Shape {
    PointShape(point: Point),
    CircleShape(circle: Circle),
    NoShape
}

@toString()
pub enum SVGLengthType {
    unknown,
    number,
    percentage,
    ems,
    exs,
    px,
    cm,
    mm,
    in,
    pt,
    pc 
}


println(Circle(Point(10, 20), 5));
println(CircleShape(Circle(Point(10, 20), 5)));
println(NoShape());
println(percentage());