printAllocated();

main();

printAllocated();

fn main() {
    println(callFn(getList()));
}

fn getList() -> fn() -> List<int> {
    let one = listOf(1);
    { push(one, 2);};
}

fn callFn(f: fn() -> List<int>) -> List<int> {
    f();
}