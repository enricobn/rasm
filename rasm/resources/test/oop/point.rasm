struct Point {
    x: i32,
    y: i32
}

fn toString(p: Point) -> str {
    "Point(".add(p.x).add(",").add(p.y).add(")");
}

fn Shape(point: Point) -> Shape {
    Shape(
        { -> point; },
        { dx, dy ->
            let result = Point(point.x.add(dx), point.y.add(dy));

            Shape(result);
        }, { ->
            toString(point);
    });
}