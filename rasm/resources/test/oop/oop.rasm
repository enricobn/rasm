include "std.rasm"

include "circle.rasm"
include "rect.rasm"
include "point.rasm"
include "shape.rasm"

let v = vecOf(Shape(Circle(1, 1, 2)), Shape(Rect(10, 20, 5, 5)), Shape(Point(4, 5)));

println(v);

println(map(v, { shape -> shape.move(100, 100);}));

// TODO println(map(v, { shape -> center(shape);})); does not work
let v1 = map(v, { shape -> center(shape);});

println(v1);