pub struct Shape {
    center: fn() -> Point,
    move: fn(i32,i32) -> Shape,
    toString: fn() -> str
}

pub fn toShape<T>(inner: T) -> Shape {
    Shape(
        fn() { inner.center; },
        fn(dx, dy) { inner.move(dx, dy);},
        fn() { inner.toString; }
    );
}