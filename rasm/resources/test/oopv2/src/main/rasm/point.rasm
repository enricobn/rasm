pub struct Point {
    x: i32,
    y: i32
}

pub fn center(point: Point) -> Point {
    point;
}

pub fn move(point: Point, dx: i32, dy: i32) -> Shape {
    toShape(Point(point.x.add(dx), point.y.add(dy)));
}

pub fn toString(p: Point) -> str {
    "Point(".add(p.x).add(",").add(p.y).add(")");
}