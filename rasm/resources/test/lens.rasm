@toString()
struct Point {
    x: int,
    y: int
}

@toString()
struct Circle {
    center: Point,
    radius: int
}

@toString()
struct Shape {
    circle: Circle
}

@toString()
struct Shapes {
    shape: Shape
}

let c = Circle(Point(2, 3), 10);
let c1 = c.update(center, x, add.curry(1));
println(c1);
let c2 = c.set(center, x, 10);
println(c2);

let s = Shape(Circle(Point(2, 3), 10));
let s1 = s.update(circle, center, x, add.curry(1));
println(s1);
let s2 = s.set(circle, center, x, 10);
println(s2);

let ss = Shapes(Shape(Circle(Point(2, 3), 10)));
let ss1 = ss.update(shape, circle, center, x, add.curry(1));
println(ss1);
let ss2 = ss.set(shape, circle, center, x, 10);
println(ss2);