// time ./fibonacci 40
// 102334155

//real    0m4,345s

// with lambda space ref / deref
// real    0m6,390s

// optimized lambda space ref /deref
// 56 bytes allocated
// 110 table slots allocated

// real    0m4,716s


include "std.rasm"

println(fib(atoi(argv(1))));

fn fib(n: i32) -> i32 {
    if(lessOrEqual(n, 1),n,{ ->
        add(fib(add(n,-1)), fib(add(n,-2)));
    });
}

/*
asm ff(n: i32) -> i32 /{
    push    ebx
    mov     eax,$n
    cmp     eax, 1
    jbe     .end

    sub     eax, 1
    push    eax
    call    ff
    add     esp, 4
    mov     ebx,eax

    mov     eax,$n
    sub     eax,2
    push    eax
    call    ff
    add     esp, 4
    add     eax, ebx
.end:
    pop     ebx
}/
*/