enum Animal {
    Dog,
    Cat,
    Cow
}

Animal::Dog().matchDog(
    fn() { println("Found"); },
    fn() { println("NotFound"); }
);

Animal::Cow().matchDog(
    fn() { println("Found"); },
    fn() { println("NotFound"); }
);