let n = argv(1).fmap(fn(s) { s.toInt;}).getOrElse(999);

inner1(n);

fn inner1(n: int) {
    inner({ add(n, 10); });
}

fn inner(f: fn() -> int) {
    println(do(f));
}

fn do(f: fn() -> int) -> int {
    f();
}
