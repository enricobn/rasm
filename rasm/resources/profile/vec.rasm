// RUST_LOG=info RUST_BACKTRACE=full cargo run --release rasm/resources/profile/vec.rasm tmp/vec
// ./vec 1000000
// 56 bytes allocated
// real    0m0,061s
struct Point {
    x: int,
    y: int
}

main();
main();

printAllocated();

fn main() {
    let points = points(toi32(argv(1)));
    println(points.len);
}

fn points(count: int) -> Vec<Point> {
    vecRange(0, count).map({ it -> Point(it, it); });
}